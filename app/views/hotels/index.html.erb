<h1>Mes hôtels</h1>

<div class='hotel-card'>
  <h1><%= link_to 'Ajouter un hôtel', new_hotel_path %></h1>
</div>

<div class="index-hotels" style="margin:10px; display: grid;grid-template-columns: 1fr 1fr 1fr 1fr;">
    <% @hotels.each do |h| %>
    <div class='hotel-card' style="width: 200px; height: 200px; padding: 10px; border:solid 1px black; border-radius: 10px; margin-top: 10px;">
      <p><%= link_to h.hotel_name, hotel_path(h) %></p>
      <%= link_to "Supprimer cet hôtel", hotel_path(h), method: :delete %>
    </div>
  <% end %>
</div>

<div class='hotel-card'>
  <h1><%= link_to 'Ajouter un hôtel', new_hotel_path %></h1>
</div>
